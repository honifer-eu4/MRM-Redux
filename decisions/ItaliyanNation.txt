country_decisions = {
	italiyan_nation = {
		major = yes
		potential = {
			normal_or_historical_nations = yes
			was_never_end_game_tag_trigger = yes
			NOT = { has_country_flag = formed_italiya_flag }
			OR = {
				ai = no
				is_playing_custom_nation = no
			}
			OR = {
				is_free_or_tributary_trigger = yes
				ai = no
			}
			NOT = { exists = ITY }
			OR = {
				was_tag = TUN
				tag = TUN
			}
		}
		provinces_to_highlight = {
			OR = {
				AND = {
					region = italy_region
					root = {
						NOT = {
							num_of_owned_provinces_with = {
								value = 45
								region = italy_region
								is_core = root
							}
						}	
					}
				}
			}
			OR = {
				NOT = { owned_by = ROOT }
				NOT = { is_core = ROOT }
			}
		}
		allow = {
			adm_tech = 10
			is_free_or_tributary_trigger = yes
			is_at_war = no
			num_of_owned_provinces_with = {
				value = 45
				region = italy_region
				is_core = root
			}
		}
		effect = {
			if = {
				limit = {
					NOT = { government_rank = 3 }
				}
				set_government_rank = 3
			}
			change_tag = ITY
			on_change_tag_effect = yes
			#this is so that a tier 1 reform is always selected, preventing the user from just never selecting one and enjoying loads of absolutism
			if = {
				limit = { 
					NOT = { government = monarchy }
					is_revolutionary = no
				}
				change_government = monarchy
			}
			if = {
				limit = {
					NOT = { has_reform_on_level = 0 }
				}
				add_government_reform = italiyan_government
			}
			italy_region = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_permanent_claim = ITA
			}
			add_prestige = 25
			if = {
				limit = { has_custom_ideas = no }
				country_event = { id = ideagroups.1 } #Swap Ideas
			}
			swap_non_generic_missions = yes
			set_country_flag = formed_italiya_flag
		}
		ai_will_do = {
			factor = 400
			modifier = {
				factor = 0
				NOT = {
					calc_true_if = {
						owns_core_province = 101	# Genoa
						owns_core_province = 104	# Milan
						owns_core_province = 116	# Firenze
						owns_core_province = 118	# Roma
						amount = 3
					}
				}
			}
			modifier = {
				factor = 0
				NOT = {
					num_of_cities = 10
				}
			}
			modifier = {
				factor = 0
				NOT = { adm_tech = 8 }
			}
		}
	}
}